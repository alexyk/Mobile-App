import { StyleSheet } from "react-native";

export default StyleSheet.create({
  dayRow: {
    flex: 1,
    flexDirection: "row",
    alignSelf: "stretch",
    alignItems: "center",
    paddingVertical: 5
  }
});
